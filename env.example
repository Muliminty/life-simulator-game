# AI 服务配置示例
# 复制此文件为 .env.local 并填入你的实际配置
# ⚠️ 重要：.env.local 文件已添加到 .gitignore，不会被提交到 Git

# AI API Key（必需）
# 获取方式：
# - SiliconFlow: https://siliconflow.cn 注册并创建 API Key
# - OpenAI: https://platform.openai.com/api-keys 创建 API Key
VITE_AI_KEY=your-api-key-here

# AI 服务提供商（可选，默认: siliconflow）
# 可选值: siliconflow, openai, custom
VITE_AI_PROVIDER=siliconflow

# AI 模型配置（可选）
# 方式一：通过配置文件（推荐）- 编辑 config/ai.json，修改 defaultModel 字段
# 方式二：通过环境变量（临时覆盖）- 设置 VITE_AI_MODEL_ID 覆盖配置文件中的 defaultModel
# 支持的模型 ID: qwen-72b, deepseek-v3, qwen-32b, qwen-14b, llama-70b, glm-4, gpt-4, gpt-4-turbo, gpt-3.5-turbo, o1-preview
# VITE_AI_MODEL_ID=qwen-72b

# 传统方式（已废弃，建议使用配置文件）
# VITE_AI_MODEL=Qwen/Qwen2.5-72B-Instruct

# 自定义 AI 服务 URL（仅当 VITE_AI_PROVIDER=custom 时使用）
# VITE_AI_URL=https://your-custom-api-url.com/v1/chat/completions

